general {
        output_format = "i3bar"
        colors = true
        interval = 5
}

order += "load"
order += "tztime local"

load {
        format = "%1min"
}

tztime local {
        format = "%Y-%m-%d %H:%M "
}
