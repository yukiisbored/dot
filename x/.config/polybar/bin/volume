#!/bin/sh

level=$(mixer vol | sed 's/^Mixer vol[[:blank:]]*is currently set to //;s/:[[:number:]]*$//')

icon=""

[ $level -le 50 ] && icon=""
[ $level -le 20 ] && icon=""
[ $level -eq 0 ] && icon=""

printf "%s %s%%\n" "$icon" "$level"
